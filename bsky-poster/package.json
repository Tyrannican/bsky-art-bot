{
  "scripts": {
    "start": "ts-node .",
    "build": "rm -rf dist/ && tsc --build",
    "minify": "esbuild index.js --bundle --minify --platform=node --outfile=index.js --allow-overwrite"
  },
  "dependencies": {
    "@atproto/api": "^0.13.16",
    "@aws-sdk/client-dynamodb": "^3.696.0",
    "@aws-sdk/client-s3": "^3.693.0",
    "@aws-sdk/client-secrets-manager": "^3.693.0",
    "@aws-sdk/lib-dynamodb": "^3.696.0",
    "axios": "^1.7.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.4"
  }
}
